#!/bin/bash

# A simple script to display clock
# 'clock -i' = with icon, 'clock' = text only
# Cheers!
# Addy

CLOCK=$(LANG=es_ES.utf8 LC_TIME=es_ES.utf8 date +"%a %d %b / %H:%M")

	if  [[ $1 = "-i" ]]; then
    echo "  ${CLOCK}"
	else
    echo "${CLOCK}"
    fi
